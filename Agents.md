# Howmoon · Earth Dialogue 开发交接

本文件记录本仓库的架构、操作流程与维护边界。贡献者的 PR 要求见 [CONTRIBUTING.md](CONTRIBUTING.md)。规则统一维护在 `Agents.md`，不另建单数文件或入口副本。使用只自动识别全大写文件名的工具时，请在任务开头明确要求先读取本文。

## 开始工作前

1. 阅读本文、README 和任务涉及的说明，运行 `git status --short`，识别用户已有改动。不要覆盖、删除或顺手格式化无关文件。
2. 区分调查和实现。调查时先复现并解释，获得修改授权后才修复。提交、推送、正式部署和更改公开访问设置分别需要授权。
3. 找到数据源及其消费者，优先复用现有模块和脚本。新增城市通常只需要数据、素材和生成步骤，不需要引入新的框架、数据库或路由系统。
4. 不读取或输出真实 Key，只检查变量名、是否配置及脱敏错误。文档、截图和提交中不能出现私有配置、临时访问凭据或个人隐私。
5. 数量与历史结果只作基线。当前有 17 城、110 景点，其中 15 城使用统一新图鉴，北京、罗马各保留十景的现有实现。每次交付仍需重新检查。

## 架构与文件归属

根目录 `app.py` 导入 `backend/app/main.py` 的 FastAPI 实例。服务提供 API，本地通过 `StaticFiles` 提供 `site/`。新城市图鉴由数据和脚本生成；首页地球与部分旧页面使用已提交的前端 bundle。

| 位置 | 负责什么 | 修改原则 |
| --- | --- | --- |
| `site/city-atlas-data.json` | 城市、景点、配色、坐标、热点和文章 | 新图鉴内容的主要数据源 |
| `scripts/build_city_atlases.py` | 城市目录、名片和独立图鉴 HTML | 改数据或模板后生成并提交产物 |
| `site/city-atlas.js` | 名片进入、长卷选择和拖动、热点、放大镜 | 公共改动需复查其他城市 |
| `site/city-atlas.css` | 新图鉴和目录的布局、响应式样式 | 保留窄屏、焦点和减少动画支持 |
| `site/landmark-reading.css` | 新旧图鉴共用的文章排版 | 留意旧页面样式作用域 |
| `site/assets/<city>/` | 名片、长卷和单景插画 | 文件名、真实编码及 JSON 后缀一致 |
| `site/brand-overrides.css` | 何月品牌与部分旧样式覆盖 | 小范围修改，复查首页和旧图鉴 |
| `recovered/` | 受维护的分行前端 bundle | 与发布 bundle 同步，不假设存在可直接 `npm run build` 的完整前端工程 |
| `scripts/sync_city_entries.py` | 地球点亮数据、城市匹配、发布 bundle | 保留标记块与精确锚点，失败时停止排查 |
| `scripts/build_legacy_reading.py` | 北京、罗马的服务端讲解 HTML | 与旧图鉴客户端组件保持相同结构 |
| `backend/app/config.py` | 环境变量、模型、超时和等待设置 | 不硬编码凭据，默认值以代码为准 |
| `backend/app/dialogue.py` | 模型对话、故障切换、JSON 校验与 mock | 保留提供方标识和失败边界 |
| `backend/app/services.py` | 地理与语音服务 | 地理回退会读取城市元数据 |
| `backend/app/visuals.py` | 对话后的个性化旅行水彩 | 与预制图鉴素材是独立流程 |
| `backend/tests/` | API、图鉴及部署回归 | 通过测试不等于真实模型或线上体验已验收 |
| `docs/` | 资料来源、研究、截图、验证记录 | 草稿不能未经校对直接发布 |
| `public/` | Vercel 构建生成的静态资源 | 已忽略，不作为编辑源，不提交 |

### 城市入口如何连起来

```text
城市 JSON + 配图
  → build_city_atlases.py → 城市目录 / 名片 / 图鉴 HTML
  → sync_city_entries.py → 地球快捷点亮 / 城市别名匹配

对话结果或 /?fly=<slug>
  → 地球城市光点
  → /city/<slug>/ 名片
  → ?enter=1&landmark=<slug> 长卷
  → /city/<city>/experience/<landmark>/ 图鉴与讲解
```

卡片能打开，只验证了其中一条路径。新城市还要检查目录搜索、地球点亮与匹配后的跳转。

## 新城市与内容规则

- 新完整城市按六景规格交付，通常为 1 张名片、1 张长卷、6 张单景图。改变数量时同时修改区域定义、模板中的数量文案和测试，不能只增加一条数组元素。
- 城市键和景点 slug 使用稳定的小写英文，多词用连字符。改动已发布 slug 时说明旧链接与缓存如何兼容。
- `panoramaRegions` 与 `landmarks` 同序，每项为 `[start, end, labelY]`，均为百分比。区域必须对应实际地标；东京、西安的缺省区域仍在构建脚本中，不能假定所有城市都只需改 JSON。
- `points` 默认三处，`x/y` 相对于整张图，包括纸张留白。必须人工校对，不接受重叠的 `50/50` 占位点。内部空间未画出时，在入口明确标为“入内延伸”，不能将解释贴到无关外墙。
- `article` 固定三节，分别提供独立内容。正文总计至少 200 个字符，建议约 220 至 400 个中文字，介绍背景、观察方法和漫游建议，不把热点原文重复粘贴。
- 名称、年代、结构和地理关系优先核对官方或第一方资料。`source` 放主要链接，多来源、日期和分歧写入 `docs/`。
- 坐标用于近似定位，不能标为精确测绘。机场码与城市码需核验，不能从地名猜三个字母，也不能把链接打开成功当成 Trip 已正确预填。
- 票价、开放时间、预约和修复状态可能变化，保留核验日期及官方入口，不作实时承诺。
- 新国家检查 `sync_city_entries.py` 的 `COUNTRY_CODES`，别名检查 `EXTRA_ALIASES`。该脚本特意跳过东京、西安，它们的入口仍保留在分行 bundle 中，修改时要同时核对两处内容。

## 视觉与素材规则

名片优先用俯瞰构图和当地色彩建立辨识度，保证标题与按钮可读。长卷采用连续的主题编排，单景图延续暖纸、细线与克制淡彩。推荐比例分别接近 16∶9、3∶1、3∶2；布局使用图片实际宽高，不为比例裁坏主体。

素材需交代作者、来源及可在项目中使用的依据。AI 图注明工具和提示摘要，检查地标轮廓、地形、植被与人物。工具失败或额度耗尽时保留已完成文件，不能用重复模板冒充不同城市。

- 区分实拍、艺术演绎和精确地图，保留艺术示意说明。
- 不混用相似地标。本轮曾修正镇海楼被画成细长宝塔的问题，应保留其宽阔五层楼的识别特征。
- 新资源建议单张不超过 2 MB，较大文件说明必要性。网页成品与原始大图分开保存，避免重复提交。
- 不能只修改扩展名来假装转码。验证真实编码和解码结果，尤其是 PNG 下载。
- 改图后重新校对热点及局部放大区域，不能仅凭提示词相信图片内容。

## 必须保留的交互

1. 桌面进入图鉴默认启用放大镜，鼠标移到图上即可显示。允许关闭，但不能默认关闭。
2. 触屏可点按热点阅读，不能只依赖 hover。保留按钮名称、焦点、Escape 关闭与减少动画支持。
3. 文章直接显示在图片下方，“阅读景点讲解”使用原生 `#overview` 跳转，不重新藏进折叠区域。
4. 长卷保留拖动、索引、方向控制和位置条，拖动后抑制误点击。
5. 从图鉴返回长卷恢复选中景点，名片、目录和地球之间保持清楚的返回路径。
6. 新页面不得意外横向溢出。北京、罗马原图鉴仍有大屏限制，不能据此宣称全站已适配手机。

## 北京与罗马的特殊实现

北京原图鉴位于 `/experience/<slug>/`，罗马位于 `/city/rome/experience/<slug>/`。共享客户端图鉴组件在 `site/_next/static/chunks/207-bb6e7ddfb6c92eb9.js`，场景信息由原页面 Flight 数据传入。

修改旧客户端讲解结构时，同时运行 `build_legacy_reading.py` 对齐服务端 HTML。只改客户端曾导致 React hydration mismatch。该脚本读取已有 Flight 场景，不会自动把新城市 JSON 转成旧城数据。

部分 `recovered/experience/...` 文件只是入口壳。先沿实际加载关系找到共享组件，更新后核对资源版本与缓存，不要仅凭旧页面没有变化就断言代码无效。

## 前端 bundle 维护

`sync_city_entries.py` 使用 `ATLAS_*` 标记块更新入口信息，然后移除分行文件中的换行，并应用品牌替换。不要加入会因换行消失而吞掉后续代码的 `//` 注释，不要手改标记区后期待下次生成保留。

它同步首页 bundle，以及城市目录下 `%5Bslug%5D` 和 `[slug]` 两种路径。只改发布文件可能被下次生成覆盖。锚点断言失败时先检查文件结构，不能删除断言后继续批量替换。

并非所有旧 chunk 都有分行源。必须直接修改旧发布文件时，在提交中说明原因，对实际 JavaScript 做语法检查，并验证服务端 HTML、Flight 数据与客户端输出一致。

## 后端原理与限制

### 对话和等待

`POST /api/dialogue` 向浏览器提供 SSE。当前 `_complete_provider` 会先聚合并校验完整的上游 JSON，随后 `stream_turn` 再向浏览器分段输出。不要把这个过程描述成逐 token 实时输出或思维链展示。

配置优先级为千问、DeepSeek。请求或结构校验失败后再尝试下一提供方；无可用服务时，只有 `ALLOW_MOCK_DIALOGUE` 允许才返回 `local-mock`。改动时测试故障切换、全失败和禁止 mock，不能用固定回答掩盖服务故障。

提示词及 mock 按五条用户回答进入结果阶段组织流程。提示词要求不等于服务端完整校验，换模型或改协议时需检查前端所需字段和异常分支。等待动画属于体验设计，回答速度无法证明是否调用 AI，应检查实际请求、提供方标识与配置。

### 两种图片流程

预制图鉴位于 `site/assets/`，用户对话结束后的小幅旅行水彩由 `/api/city-visual` 调用千问生成。两种素材和缓存不能混用。

`visuals.py` 仅发送筛选后的目的地及场景资料，不发送完整原始对话。保留图片地址 HTTPS/主机校验，下载时不向图片主机转发 API Key。保留体积、像素限制和真实 PNG 转码；失败返回明确错误，不静默回退到模板图。

当前生图服务端整体超时 110 秒，前端协议记录为 125 秒。风格或缓存语义变更时核对 `STYLE_VERSION`、客户端存储键及重画行为。后端没有持久数据库，不能声称已实现跨用户任务持久化或可靠队列。

### 语音、地理与机票

语音输出必须匹配客户端 PCM 格式、采样率和声道约定，不能把带 WAV 文件头或压缩过的字节直接当 PCM。macOS 本地朗读与在线语音的运行条件不同，核对实际实现及 `API_CONTRACTS.md`。

地理结果会使用候选坐标和已知城市目录，不能保证真实边界已核验。Trip 是带目的地参数的外链，本项目不查询实时票价、不出票，也不支付。

公共 API 的鉴权、成本控制、速率限制和滥用防护，不能因项目用于演示就视为已解决。涉及这些能力时单独说明设计与验证，不擅自加入付费服务或扩大数据收集范围。

## 构建与验证

安装和跨平台启动见 [贡献指南](CONTRIBUTING.md)。城市与图鉴改动从仓库根目录依次执行。

```sh
backend/.venv/bin/python scripts/build_city_atlases.py
backend/.venv/bin/python scripts/sync_city_entries.py
backend/.venv/bin/python scripts/build_legacy_reading.py
backend/.venv/bin/python -m pytest backend/tests -q
node --check site/city-atlas.js
node --check site/city-catalogue.js
node --check site/_next/static/chunks/app/page-cfec76f4fe91be41.js
git diff --check
```

其他 JavaScript 改动也要对实际文件运行 `node --check`。脚本应可重复运行，第二次执行不产生无关变化。源数据、生成页面和对应 bundle 一起提交，`site/` 有变化时更新 `SHA256SUMS.txt`，命令见贡献指南。

只改文档无需重生成前端或调用真实 AI。默认使用 mock 与测试传输，不消耗用户 Key。需要真实模型、语音或生图验证时，先明确授权、成本和发送内容。

浏览器验证目录、地球点亮、名片、长卷选择、默认放大镜、热点、文章和返回位置。新增城市需逐个查看六景，公共交互变化需抽测未修改城市。静态路由测试不能替代视觉和操作检查。

交付时区分本次已测、未测、本地提交、远端推送、部署 Ready 和线上验收。没有请求的日志不能证明线上无错误，历史通过记录不能当作本次结果。提交使用贡献者自己的 GitHub 身份。

## Vercel 部署

构建入口为 `vercel.json` 中的 `python scripts/prepare_vercel.py`。

- 本地默认复制 `site/assets/` 至生成目录 `public/assets/`，原目录不动。
- `VERCEL=1` 时在云端临时 checkout 中移动图片。图片交给静态资源服务，页面、脚本和 `city-atlas-data.json` 留给应用使用。
- 不要在本地真实工作目录随意设置 `VERCEL=1`，不要把整个仓库复制进 `public/`，不要提交生成副本。
- 云端移动拒绝覆盖已有目标目录。失败时先检查环境与来源，不能删除宽泛目录，也不能把本地缺图当作可丢弃原图的理由。
- 不要把整个 `site/` 从函数包排除。后端导入时要读取 `city-atlas-data.json`，缺少它可能造成线上启动失败。

本轮曾遇到约 260.83 MB 的函数包超过当次构建器的 225 MB 限制。仅增加 `excludeFiles` 没有解决问题，云端移动图片后成功构建的函数约 39.66 MB。这些是当时日志结果，不是所有计划的永久上限。增加大批资源后重新检查实际包大小。

部署前确认远端 SHA、项目与环境，不在指南中记录 Key、临时访问参数或本机认证文件。区分 Git 网络错误、构建错误和运行时错误，不能为解决构建问题直接关闭访问保护。

Ready 后检查正式域名、目录、新图鉴、静态图片及健康接口。DNS/连接超时时记录线上未验证，不能直接判断服务故障，也不能宣称线上验收通过。日志没有采样请求时保留该边界。

## 历史问题速查

| 情况 | 处理原则 |
| --- | --- |
| 小画反复相似 | 区分预制素材和个性化生成，检查提示、缓存与风格版本 |
| 导出图片损坏 | 验证字节与解码，真实转为 PNG，不能只改文件名 |
| 目录能打开但地球无入口 | 检查入口同步、国家代码和别名 |
| 热点解释与画面不符 | 依成图标坐标，内部延伸阅读明确说明 |
| 旧图鉴添加文章后 React 报错 | 对齐服务端 HTML、客户端结构和 Flight 数据，检查缓存 |
| 阅读跳转不稳定 | 优先原生锚点，检查真实滚动位置，排除自动化工具问题 |
| 自动化返回未导航 | 选中真实链接，正确引用带空格的选择器，检查实际 URL |
| Git 推送超时 | 核对远端 SHA 后安全重试，提交成功不等于推送成功 |
| 图片让函数包超限 | 检查临时构建目录移动，保留原图与后端必需元数据 |

更多细节见 [API 协议](API_CONTRACTS.md)、[图鉴开发说明](docs/city-atlases.md) 和 [验证记录](docs/city-atlas-verification.md)。修改约定时同步更新本文和贡献指南，避免后来者照着过时说明操作。
